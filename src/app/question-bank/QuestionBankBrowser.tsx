'use client'

import Link from 'next/link'
import {useEffect,useMemo,useRef,useState} from 'react'

type BankQuestion={id:string;prompt:string;choices:string[];correct_index:number;content_area:string|null;subject_category:string|null;chapter_number:number|null;chapter_title:string|null;source_type:string;shared_question_id:string|null;imported_collection_id:string|null;collection_title:string|null;collection_section:string|null}
type Section={key:string;title:string;questions:BankQuestion[]}
type Group={title:string;sections:Section[]}

function normalize(value:string){return value.toLowerCase().replace(/\s+/g,' ').trim()}
function chapterLabel(q:BankQuestion){if(q.chapter_number)return `Chapter ${q.chapter_number}${q.chapter_title?` — ${q.chapter_title}`:''}`;if(q.chapter_title)return q.chapter_title;return 'Unassigned'}
function subjectLabel(q:BankQuestion){return q.subject_category||q.content_area||q.collection_section||'Unassigned'}
function SelectionBox({checked,indeterminate,onChange,label}:{checked:boolean;indeterminate?:boolean;onChange:(checked:boolean)=>void;label:string}){const ref=useRef<HTMLInputElement>(null);useEffect(()=>{if(ref.current)ref.current.indeterminate=Boolean(indeterminate)},[indeterminate]);return <label className="bank-tree-check" onClick={e=>e.stopPropagation()}><input ref={ref} type="checkbox" checked={checked} onChange={e=>onChange(e.target.checked)}/><span>{label}</span></label>}

export default function QuestionBankBrowser({questions,deleteAction,bulkAction}:{questions:BankQuestion[];deleteAction:(id:string)=>Promise<void>|void;bulkAction:(formData:FormData)=>Promise<void>|void}){
 const[search,setSearch]=useState('');const[selected,setSelected]=useState<Set<string>>(()=>new Set());const needle=normalize(search)
 const filtered=useMemo(()=>questions.filter(q=>!needle||normalize(`${q.prompt} ${chapterLabel(q)} ${subjectLabel(q)} ${q.collection_title??''} ${q.collection_section??''} ${q.source_type??''}`).includes(needle)),[questions,needle])
 const subjectOptions=useMemo(()=>[...new Set(questions.map(subjectLabel).filter(x=>x!=='Unassigned'))].sort((a,b)=>a.localeCompare(b)),[questions])
 const chapterTitleOptions=useMemo(()=>[...new Set(questions.map(q=>q.chapter_title?.trim()).filter((x):x is string=>Boolean(x)))].sort((a,b)=>a.localeCompare(b)),[questions])
 const groups=useMemo<Group[]>(()=>{const byBundle=new Map<string,Map<string,BankQuestion[]>>();for(const q of filtered){const bundle=q.collection_title||'Custom';const chapter=chapterLabel(q);const subject=subjectLabel(q);const key=`${chapter}|||${subject}`;if(!byBundle.has(bundle))byBundle.set(bundle,new Map());const sections=byBundle.get(bundle)!;sections.set(key,[...(sections.get(key)||[]),q])}return[...byBundle.entries()].map(([title,sections])=>({title,sections:[...sections.entries()].map(([key,items])=>{const[chapter,subject]=key.split('|||');return{key,title:`${chapter} · ${subject}`,questions:items}}).sort((a,b)=>a.title.localeCompare(b.title,undefined,{numeric:true}))}))},[filtered])
 function setMany(ids:string[],checked:boolean){setSelected(current=>{const next=new Set(current);ids.forEach(id=>checked?next.add(id):next.delete(id));return next})}
 const selectedVisible=filtered.filter(q=>selected.has(q.id)).length
 return <>
  <section className="bank-tree-toolbar"><input className="bank-tree-search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search chapter, subject, question, or source…" autoComplete="off"/><div className="bank-tree-status"><span>{filtered.length} of {questions.length} questions</span>{selected.size>0&&<span> · {selected.size} selected</span>}{search&&<button className="bank-clear-search" type="button" onClick={()=>setSearch('')}>Clear</button>}</div></section>

  {selected.size>0&&<form action={bulkAction} className="card bank-bulk-editor">
    {[...selected].map(id=><input type="hidden" name="question_ids" value={id} key={id}/>)}
    <div className="row between bank-bulk-heading"><div><h2>Organize {selected.size} selected question{selected.size===1?'':'s'}</h2><p className="muted">Fill only the fields you want to change. Existing chapter and subject values are suggested, but new values are allowed.</p></div><button className="ghost" type="button" onClick={()=>setSelected(new Set())}>Clear selection</button></div>
    <div className="bank-bulk-grid">
      <div><label>Chapter number</label><input name="chapter_number" type="number" min="1" step="1" placeholder="Example: 4"/></div>
      <div><label>Chapter title</label><input name="chapter_title" list="bank-chapter-titles" placeholder="Select or create a chapter"/><datalist id="bank-chapter-titles">{chapterTitleOptions.map(title=><option value={title} key={title}/>)}</datalist></div>
      <div><label>Subject category</label><input name="subject_category" list="bank-subject-categories" placeholder="Select or create a subject"/><datalist id="bank-subject-categories">{subjectOptions.map(subject=><option value={subject} key={subject}/>)}</datalist></div>
    </div>
    <div className="row bank-bulk-actions"><button type="submit">Apply to {selected.size}</button><span className="muted">The smart-test builder will use these values immediately.</span></div>
  </form>}

  {!filtered.length?<section className="card"><p className="muted">No saved questions match your search.</p></section>:<div className="bank-tree">{groups.map(bundle=>{const bundleIds=bundle.sections.flatMap(s=>s.questions.map(q=>q.id));const bundleSelected=bundleIds.filter(id=>selected.has(id)).length;const bundleAll=bundleIds.length>0&&bundleSelected===bundleIds.length;return <details className="bank-bundle" key={bundle.title} open={Boolean(needle)}><summary className="bank-bundle-summary"><div className="bank-tree-title"><b>{bundle.title}</b><span>{bundleIds.length} questions · {bundle.sections.length} chapter/subject groups</span></div><SelectionBox checked={bundleAll} indeterminate={bundleSelected>0&&!bundleAll} onChange={checked=>setMany(bundleIds,checked)} label="All"/></summary><div className="bank-section-list">{bundle.sections.map(section=>{const ids=section.questions.map(q=>q.id);const count=ids.filter(id=>selected.has(id)).length;const all=count===ids.length;return <details className="bank-section" key={section.key} open={Boolean(needle)}><summary className="bank-section-summary"><div className="bank-tree-title"><b>{section.title}</b><span>{section.questions.length}</span></div><SelectionBox checked={all} indeterminate={count>0&&!all} onChange={checked=>setMany(ids,checked)} label="All"/></summary><div className="bank-question-list">{section.questions.map(q=>{const choices=Array.isArray(q.choices)?q.choices:[];return <div className="bank-question-row" key={q.id}><label className="bank-question-select"><input type="checkbox" checked={selected.has(q.id)} onChange={e=>setMany([q.id],e.target.checked)}/><span>{q.prompt}<small>{chapterLabel(q)} · {subjectLabel(q)}</small></span></label><div className="bank-question-actions"><details><summary>Preview</summary><ol type="A">{choices.map((choice:string,i:number)=><li key={i}><span className={i===q.correct_index?'good':''}>{choice}{i===q.correct_index?' ✓':''}</span></li>)}</ol></details><Link href={`/question-bank/${q.id}/edit`}>Edit</Link><form action={deleteAction.bind(null,q.id)}><button className="bank-text-button danger">Remove</button></form></div></div>})}</div></details>})}</div></details>})}</div>}

  {selected.size>0&&<div className="bank-selection-bar"><span><b>{selected.size} selected</b>{needle&&<span className="muted"> · {selectedVisible} visible</span>}</span><a href="#" onClick={e=>{e.preventDefault();document.querySelector('.bank-bulk-editor')?.scrollIntoView({behavior:'smooth',block:'center'})}}>Edit chapter & subject ↑</a></div>}
  <style jsx global>{`.bank-tree-toolbar{position:sticky;top:8px;z-index:10;background:#f6f7fb;padding:8px 0 10px;margin:14px 0 6px}.bank-tree-search{margin:0;padding:11px 13px;background:#fff}.bank-tree-status{display:flex;align-items:center;gap:4px;min-height:26px;margin-top:5px;color:#64748b;font-size:12px}.bank-clear-search{margin-left:auto;background:transparent;color:#4338ca;padding:3px 6px;font-size:12px}.bank-bulk-editor{border:1px solid #c7d2fe;background:#eef2ff}.bank-bulk-editor h2{margin:0 0 3px;font-size:18px}.bank-bulk-heading{align-items:flex-start;gap:12px}.bank-bulk-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-top:12px}.bank-bulk-grid label{font-size:12px}.bank-bulk-grid input{background:#fff}.bank-bulk-actions{align-items:center;gap:10px;margin-top:12px}.bank-tree{display:flex;flex-direction:column;gap:9px}.bank-bundle,.bank-section{background:#fff;border:1px solid #e4e7ef;border-radius:13px;overflow:hidden}.bank-bundle-summary,.bank-section-summary{list-style:none;display:flex;align-items:center;justify-content:space-between;gap:12px;cursor:pointer;padding:13px 14px}.bank-bundle-summary::-webkit-details-marker,.bank-section-summary::-webkit-details-marker{display:none}.bank-bundle-summary:before,.bank-section-summary:before{content:'›';font-size:20px;color:#64748b;transition:.15s;flex:0 0 auto}.bank-bundle[open]>.bank-bundle-summary:before,.bank-section[open]>.bank-section-summary:before{transform:rotate(90deg)}.bank-section-list{padding:0 10px 10px;background:#f8fafc;border-top:1px solid #eef0f4}.bank-section{margin-top:9px;border-radius:10px}.bank-section-summary{padding:10px 12px}.bank-tree-title{min-width:0;flex:1;display:flex;align-items:baseline;gap:8px}.bank-tree-title b{font-size:14px}.bank-tree-title span{color:#64748b;font-size:11px}.bank-tree-check{display:flex;align-items:center;gap:5px;font-size:11px;font-weight:700;color:#475569;cursor:pointer}.bank-tree-check input,.bank-question-select input{width:auto;margin:0;accent-color:#4338ca}.bank-question-list{border-top:1px solid #eef0f4}.bank-question-row{padding:9px 11px;border-bottom:1px solid #f0f2f6}.bank-question-select{display:grid;grid-template-columns:18px 1fr;gap:8px;align-items:start;font-size:13px;font-weight:550;line-height:1.35;cursor:pointer}.bank-question-select small{display:block;color:#64748b;font-size:11px;font-weight:500;margin-top:3px}.bank-question-actions{display:flex;align-items:flex-start;gap:10px;margin:5px 0 0 26px;font-size:11px}.bank-question-actions a,.bank-question-actions summary,.bank-text-button{color:#4338ca;font-size:11px;font-weight:700;cursor:pointer}.bank-question-actions details>summary{list-style:none}.bank-question-actions ol{margin:7px 0 5px;padding:8px 8px 8px 28px;background:#f8fafc;border-radius:8px;color:#475569;font-size:12px}.bank-question-actions form{margin:0}.bank-text-button{padding:0;background:transparent;border:0}.bank-text-button.danger{color:#b91c1c}.bank-selection-bar{position:sticky;bottom:12px;display:flex;justify-content:space-between;align-items:center;gap:12px;background:#111827;color:#fff;border-radius:12px;padding:10px 12px;margin-top:12px;box-shadow:0 12px 30px #11182733;font-size:13px}.bank-selection-bar .muted{color:#cbd5e1}.bank-selection-bar a{color:#fff;font-weight:700;font-size:12px}@media(max-width:720px){.bank-bulk-grid{grid-template-columns:1fr}.bank-bulk-actions{align-items:flex-start;flex-direction:column}.bank-tree-title{display:block}.bank-tree-title b,.bank-tree-title span{display:block}.bank-tree-check span{display:none}}`}</style>
 </>
}
