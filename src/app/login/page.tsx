import Link from 'next/link'
import { login, signup } from './actions'
export default async function Login({searchParams}:{searchParams:Promise<{error?:string,message?:string}>}){const q=await searchParams;return <main className="narrow"><Link href="/">← Home</Link><h1>Sign in</h1><p className="muted">Teachers and students use the same secure sign-in. Choose an account type only when creating a new account.</p>{q.error&&<p className="bad notice">{q.error}</p>}{q.message&&<p className="good notice">{q.message}</p>}<form className="card"><label>Name <span className="muted">(new accounts)</span></label><input name="full_name"/><label>Email</label><input name="email" type="email" required/><label>Password</label><input name="password" type="password" minLength={6} required/><label>Account type <span className="muted">(new accounts)</span></label><select name="role"><option value="student">Student</option><option value="teacher">Teacher</option></select><div className="row"><button formAction={login}>Sign in</button><button className="secondary" formAction={signup}>Create account</button></div></form></main>}
